<h1>Update Pet</h1>

<form method="POST" action="/pets/<%=@pet.id%>">

  <label>Name:</label>
  <input type="text" name="pet[name]" value="<%=@pet.name%>" id="pet_name">
  <br></br>

  Choose an existing owner:
  <% Owner.all.each do |owner|%>
    <input type="radio" name="pet[owner_id]" id="<%=owner.id%>" value="<%=owner.id%>" <%='checked' if owner.pets.include?(@pet) %>><%=owner.name%></input>
  <%end%>
  <br></br>
  and/or create a new owner:
  <label>Name:</label>
  <input type="text" name="owner[name]" id="owner_name" />
  <br></br>

  <input type="submit" value="Update Pet">
</form>
